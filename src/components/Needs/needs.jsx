import React from "react";

const Need = props => {
  return (
    <div>
      {/* <p>Need id: {props.need.id}</p> */}
      <h3>Need name: {props.need.name}</h3>
    </div>
  );
};

export default Need;
