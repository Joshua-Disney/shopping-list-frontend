import React from "react";

const Want = props => {
  return (
    <div>
      {/* <p>Want id: {props.want.id}</p> */}
      <h3>Want name: {props.want.name}</h3>
    </div>
  );
};

export default Want;
