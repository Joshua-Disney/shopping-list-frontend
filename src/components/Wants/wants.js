import React from "react";

const Want = props => {
  return (
    <div>
      {/* <p>Want id: {props.want.id}</p> */}
      <p>Want name: {props.want.name}</p>
    </div>
  );
};

export default Want;
